spring:
  application:
    name: ez-poop-backend
  datasource:
    url: "jdbc:postgresql://localhost:5432/ez_poop"
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
